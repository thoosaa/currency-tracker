import styled from "styled-components";

import {LayoutContainer} from "components/Layout/Layout.styled";

export const SearchMapContainer = styled(LayoutContainer)`
  background: ${({theme}) => theme.background};
`;
