import {QuoteBlock} from "components/QuoteBlock/QuoteBlock";
import {PageTemplate} from "pages/PageTemplate";

export const Home = () => {
  return (
    <PageTemplate>
      <QuoteBlock />
    </PageTemplate>
  );
};
